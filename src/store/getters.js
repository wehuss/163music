import state from './states'

export const isLogin = () => state.isLogin

export const userInfo = () => state.userInfo

export const isShow = () => state.isShow

export const playList = () => state.playList

export const mv = () => state.mv

export const commentId = () => state.commentId

export const songList = () => state.songList

export const playIndex = () => state.playIndex

export const full = () => state.full

export const playing = () => state.playing
